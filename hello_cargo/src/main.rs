fn main() {
    // cargo new hello_cargo
    //   Creates a new Rust project with directory structure,
    //   Cargo.toml, src/main.rs, and initializes git repo
    //
    // cargo run
    //   Compiles and runs the executable in one step
    //
    // cargo check
    //   Quickly checks code for errors without producing an
    //   executable (faster than cargo build)
    //
    // cargo build
    //   Compiles code and creates executable in target/debug/
    //   Use --release flag for optimized production builds
    //
    // cargo update
    //   Updates dependencies to latest compatible versions
    //   specified in Cargo.toml and updates Cargo.lock
    
    println!("Hello, world!");
}
